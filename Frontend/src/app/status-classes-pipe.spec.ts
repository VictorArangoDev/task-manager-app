import { StatusClassesPipe } from './status-classes-pipe';

describe('StatusClassesPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusClassesPipe();
    expect(pipe).toBeTruthy();
  });
});
